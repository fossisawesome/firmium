{
  "name": "firmium-desktop",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build",
    "build:arch": "tauri build && env -i HOME=$HOME PATH=$PATH TERM=$TERM PKGDEST=\"src-tauri/target/release/bundle/arch\" BUILDDIR=\"src-tauri/target/release/bundle/arch/build\" makepkg -f"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  }
}